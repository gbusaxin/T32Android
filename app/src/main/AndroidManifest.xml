<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.t32android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.T32Android"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".presentation.activities.AskActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.activities.SettingActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.activities.AnaliticActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.activities.TrainingActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.activities.MenuActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.activities.SignInActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".presentation.activities.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>